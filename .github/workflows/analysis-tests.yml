name: analysis-tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Test CASE 01
        run: python -m unittest discover -s cases/01_retail_customer_analytics/tests -v

      - name: Test CASE 02
        run: python -m unittest discover -s cases/02_youth_migration_dynamics/tests -v
